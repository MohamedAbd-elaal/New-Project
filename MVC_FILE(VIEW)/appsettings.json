{
  "Logging": {
    "Debug": {
      "LogLevel": {
        "Default": "Trace",
        "MVC_File.Controllers.HomeController": "Trace",
        "MVC_File.Controllers.ISqlEmployee": "Trace",
        "Microsoft": "Trace",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  },

  "LogLevel": {
    "Default": "Critical",
    "MVC_File.Controllers.HomeController": "Critical",
    "MVC_File.Controllers.ISqlEmployee": "Critical",
    "Microsoft": "Critical",
    "Microsoft.Hosting.Lifetime": "Information"
  },
  "AllowedHosts": "*",
  //we can run this db in secerts.json from property of our project (it is out of files of my project)
  //we can use from property(Edit project file to show us user Secertid)
  //and there is an environment in our system and when we write EmployeeDBConnection: one slash (\) not (\\) and get out of prog and open it again it will run 
  // and cli
  "ConnectionStrings": {
    "EmployeeDBConnection": "server=(localdb)\\MSSQLLocaLDB;database=EmployeeDB;Trusted_Connection=true"

  }

}
