<!--(add sign at) MVC_FILE_VIEW_.Model.Employee;// refrence or directive for Strongly Type see screenshoot(4)
<!-- model" And Model not the object we pass---->
<!--model MVC_FILE_VIEW_.ViewModel.HomeDetails2ViewModel-->
<!--Using A Global Directive NameSpace or Settings in (_ViewImport)-->
@model HomeDetails2ViewModel;
@{
    var photopath = "~/Images/" +(Model.Employee.photopath ??"img.JPG");
}

@*@{
    // we use _Viewlist (run layout over Views Folder)
    //Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Hello From _Layout Page";
    // no tags (html,head,body) because of layout


}
   

        <h4>@ViewData["Title"]</h4>
        <!--block cs-->
        @{
                var employee = ViewData["Employee"] as MVC_FILE_VIEW_.Model.Employee;//type cast for(as) Employee to know it
         }
        <div>
                ID: @employee.Id
        </div>
        <div>
                Name: @employee.Name
        </div>
       <h2>ViewBag</h2> 
       <h5>@ViewBag.Title</h5>
       <div>
                ID: @ViewBag.Employee.Id
        </div>
        <div>
                Name: @ViewBag.Employee.Name
        </div>
        <!--
        <h2>Strongly type</h2>
        
        <div>
                ID: Model.Id (add sign at)
        </div>
        <div>
                Name: Model.Name (add sign at)
        </div>
        -->
        <h2>@Model.titleClass</h2>
         <div>
                ID: @Model.Employee.Id
        </div>
        <div>
                Name: @Model.Employee.Name
        </div>
        
        @section Scripts
        {
            <script src="~/wwwroot/CustomScript.js"></script>
        }
*@
<div class="row justify-content-center m-3">
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header">
                <h1>@Model.Employee.Name</h1>
            </div>
            <div class="card-body text-center">
                <img class="card-img-top imageThumbnail"src=@photopath asp-append-version="true"/>
                <h4>Employee ID : @Model.Employee.Id</h4>
                <h4>Employee Hoppies : @Model.Employee.Hoppies</h4>
                <h4>Employee Name : @Model.Employee.Name</h4>

            </div>
            <div class="card-footer text-center">
                
                        @*<a href="/home/IEnumerable" class="btn btn-primary">Back</a>*@
                        <a asp-controller="home" asp-action="Ienumerable" class="btn btn-primary">Back</a>
                        <a  class="btn btn-primary" asp-action="edit" asp-controller="home" asp-route-id="@Model.Employee.Id">Edit</a>
                        <a href="#" class="btn btn-danger">Delete</a>
             </div>
        </div>
    </div>
</div>

    